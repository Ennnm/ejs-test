<html>
  <head>
    <!-- AFTER HTML loads in browser, browser will request for CSS. -->
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <h1><%= fruit.color%></h1>
    <h2><%= fruit.name %></h2>
    <% users.forEach( (user) => { %> 
      <li><%= user.name %></li>
      <% }); %>
  </body>
</html>